{% if template contains 'collection' %}
  {% assign filter_menu = collection.handle | append: "-filter-group-categories" %}

  {% unless pages[filter_menu].empty? or pages[filter_menu].content == blank %}
      {{ pages[filter_menu].content }}
  {% endunless %}
{% endif %}